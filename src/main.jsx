import React from "react";
import ReactDOM from "react-dom/client";
import JobPortal from "./assets/JobPortal";
import "./assets/style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JobPortal />
  </React.StrictMode>
);